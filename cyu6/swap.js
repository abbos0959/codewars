const swap=(n)=>{
    let abbos =n.split("")
    // console.log(abbos);
     salom=abbos.map((value)=>{ return value===value.toUpperCase() ? value.toLowerCase() : value.toUpperCase()}).join("")
    console.log(salom);
    
    console.log(al);

}
swap('CodeWars')